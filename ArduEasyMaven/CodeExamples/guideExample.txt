house
{
    int fff = 5.2
    Temp = PIN A 12 as input
    CurrentTime = PIN A 13 as input
    float bob2 = 5 + 8.50
    int hej = 10+5
    int hejhej = 2
    bob2 = 2
    bob2 = 2.5
    time SavedTime = 00:00
    time NextTime = 12:00

    room Kitchen
    {
        Lamp1 = PIN 1 as inputpullup
        Lamp2 = PIN 2 as inputpullup
        CoffeeMachine = PIN 3 as output
        Button = PIN 4 as input
        Window = PIN 5 as output

        Lamps = {Lamp1, Lamp2}
    }

    room Bedroom
    {
        Blinds1 = PIN 6 as output
        Blinds2 = PIN 7 as output
        Blinds3 = PIN 8 as output

        Blinds = {Blinds1, Blinds2, Blinds3}
    }
}

when (house.Kitchen.Button is true)
{
    if ((5 > 3) == true)
    {

    }

    house.Temp = 55
    float Fahrenheit = CelciusToFahrenheit(house.Temp)
    house.Kitchen.CoffeeMachine = true
    bool test = SavedTime == NextTime

    if (SavedTime + 00:15 is NextTime && Fahrenheit is above or equal 64 and test)
    {
        house.Kitchen.Window = true //Opens the window 15 mins after the coffee machine turned on if Fahrenheit >= 64.0
    }
}

when (SavedTime is 07:00)
{
    house.Bedroom.Blinds = true //This will open the blinds at 7.
    house.Bedroom.Blinds = house.Kitchen.Lamps //This will open the blinds at 7.
    house.Kitchen.Lamps = false //Turns on the Lamps in the Kitchen
}

when (SavedTime is 18:00)
{
    house.Bedroom.Blinds = false //This will close the blinds at 18.
    int bob = 0
    int val = 5
    float bobjens = 50.25 * 32 * -1.4
    DetVirkerIkke(val)
    DetVirkerIkke(bob)
    float bobbobbob = CelciusToFahrenheit(bob)
    switch (val)
    {
        case 1:
        {
            bob = 88
        }
        case 2:
        {
            if (true)
            {
            }
        }
        case 5:
        {
            bob = bob + val
        }
        default:
        {
            val = val + val
        }
    }
}

function float CelciusToFahrenheit(int temp)
{
    float result = temp * 1.8 + 32
    day weekday = MONDAY
    month test = DECEMBER
    percent anothertest = 35%
    int bob = 4
    int i = 5

    for (i = 0; i < 10; i = i + 1)
    {
        int jens = (5 + 5) * 8
    }
    for(int j = 0; j < 10; j = j + 1)
    {
        bool alec = true
        bool magnus = false

        bool party = (alec or magnus) and house.Bedroom.Blinds1
    }
    perform 158 times
    {

    }
    perform bob times
    {

    }
    perform until (i > 15)
    {

    }

    return result
}

function void DetVirkerIkke(int lol)
{
    int ajens = lol
}